# Godex

Godex is a Codex plugin starter initialized for marketplace packaging.

The plugin is intentionally tiny at `0.0.1`: manifest metadata plus a funny logo. Add skills, MCP servers, apps, or hooks only when there is real behavior to ship.
